{
  "name": "railgun_module",
  "version": "0.1.0",
  "description": "RAILGUN private transactions (shield / private transfer / unshield) for the EVM wallet — wraps the native railgun-rs engine. Sepolia-first; UNAUDITED upstream.",
  "type": "core",
  "category": "evm-wallet",
  "dependencies": ["eth_rpc_module", "keystore_module"],
  "concurrency": "single",
  "interface": "cdylib",
  "main": "railgun_module_plugin",
  "codegen": {
    "rust": {
      "crate": "rust-lib",
      "trait": "RailgunModule",
      "source": "src/glue.rs"
    }
  },
  "nix": {
    "rust": {
      "toolchain": "1.96.0",
      "packages": {
        "build": ["cmake", "pkg-config", "rustPlatform.bindgenHook"],
        "runtime": []
      },
      "env": {
        "postPatch": "bash ./patch-kohaku-witness-backend.sh && bash ./patch-kohaku-engine-seam.sh"
      }
    },
    "external_libraries": [],
    "packages": { "build": [], "runtime": [] },
    "cmake": { "find_packages": [], "extra_include_dirs": [] }
  }
}
